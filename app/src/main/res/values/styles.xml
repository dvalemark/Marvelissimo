<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blueGrey</item>
        <item name="colorPrimaryDark">@color/darkRed</item>
        <item name="actionBarTheme">@color/darkRed</item>
        <item name="toolbarNavigationButtonStyle">@color/darkRed</item>
        <item name="actionModeBackground">@color/darkRed</item>
        <item name="android:itemBackground">@color/blueGrey</item>

        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>
        <item name="android:actionMenuTextColor">t</item>

    </style>



    <!-- ActionBar styles -->
    <style name="MyActionBar"
           parent="@style/Theme.AppCompat.Holo.ActionBar">
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText"
           parent="@style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/actionbar_text</item>
    </style>

    <!-- ActionBar tabs text styles -->
    <style name="MyActionBarTabText"
           parent="@style/Widget.Holo.ActionBar.TabText">
        <item name="android:textColor">@color/actionbar_text</item>
    </style>






</resources>
